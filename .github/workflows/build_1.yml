name: Build
on:
  pull_request:
    branches: [ master ]
jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      - name: Setup tests
        run: echo "ok"

  push_to_docker_hub:
    runs-on: ubuntu-latest
    # Build Docker image and push to Docker Hub only if the app build and test is successfull
    needs: [ build_test ]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          file: dockerfile_game_sapper
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/game_sapper:latest
  deploy:
    runs-on: ubuntu-latest
    needs: [ push_to_docker_hub ]

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        # TODO: add env vars as secrets on GitHub
        with:
          # VPS IP
          host: ${{ secrets.VPS_SSH_HOST }}
          # VPS username
          username: ${{ secrets.VPS_SSH_USERNAME }}
          # SSH key (copy it from your local machine)
          key: ${{ secrets.VPS_SSH_SECRET }}
          # SSH port
          port: ${{ secrets.VPS_SSH_PORT }}
          script: |
            echo "Pull new image"
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/game_sapper:latest
            echo "Restart the container"
            docker stop ${{ github.event.repository.name }}
            docker run --rm --name game_sapper -p 8004:8004 -e POSTGRES__DB="kts" -e POSTGRES__USER="kts_user" -e POSTGRES__PASSWORD="kts_pass" -e VK__TOKEN="vk1.a.V7LecRjeSyOcbEzwnm334tqCgyoIANhleU5A1HXJMhYysPyYIg9aGFobkSgbQMMiP9PNMjDawpW-LC0g7mdp0uSphFD08UYb6uFtlLZZl0tfTlexhyKpduUQF5o9lRwIt7zVBMr5lLoId7QIOTuJxk2jIfW3WEy89TruxPvPZUde68tYvZiFPk6YUaiwgtcDOdWOJqr3A73Z28ejTg492g" -e VK__GROUP_ID="219708566" ${{ secrets.DOCKERHUB_USERNAME }}/game_sapper:latest
            echo "Pull new image OK"
            

#            docker-compose -f /home/${{ secrets.VPS_SSH_USERNAME }}/kts_winter_2023_4_xo/docker-compose.yml pull
#            echo "Restart the container"
#            docker-compose -f /home/${{ secrets.VPS_SSH_USERNAME }}/kts_winter_2023_4_xo/docker-compose.yml up --force-recreate --build --no-deps -d
#            echo "Delete old image"
#            docker image prune -f
